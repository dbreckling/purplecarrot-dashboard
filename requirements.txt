# Web service (app.py) uses stdlib only — no install needed.
# Report scripts (purplecarrot_report_v2.py, purplecarrot_v3_aggregator.py)
# need these for the Render cron jobs:
requests
pandas
pgeocode
